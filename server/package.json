{
  "name": "server",
  "version": "1.0.0",
  "type": "commonjs",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node -r dotenv/config api.js",
    "dev": "node api.js",
    "test-server": "json-server --watch testData.json --routes routes.json -w -p 8000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
